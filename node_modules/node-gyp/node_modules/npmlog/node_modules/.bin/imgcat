#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../ansi/examples/imgcat/index.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../ansi/examples/imgcat/index.js" "$@"
  ret=$?
fi
exit $ret

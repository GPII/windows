#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../ansi/examples/starwars.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../ansi/examples/starwars.js" "$@"
  ret=$?
fi
exit $ret

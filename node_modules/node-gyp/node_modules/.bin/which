#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../which/bin/which" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../which/bin/which" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../node-gyp/bin/node-gyp.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../node-gyp/bin/node-gyp.js" "$@"
  ret=$?
fi
exit $ret
